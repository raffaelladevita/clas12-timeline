'dataset' is a name given to the data set to be analyzed; it
appears as a suffix to several file names & directories

mkdir outdat.$dataset
mkdir outhipo.$dataset

start with monsub files & fcdata.$dataset.json
readData.groovy (recommend redirecting stderr somewhere, to watch for errors)
-> outdat.$dataset/data_table.dat

   |               \
   |                \
   |                 \
   V                  V

mkTree.sh               plotFiller.groovy
-> tree.root
define epoch lines       -> outhipo.$dataset/plots.hipo
-> epoch.$dataset.txt

   |                  /
   |                 /
   |                /
   V               V

qaCutter.groovy
- concatenate plots into 'epoch plots' with x axis set to runnum + filenum/5000
- for each epoch plot, calculate IQR cuts
- for each run plot, apply cuts and filter out outliers
-> outhipo.$dataset/QA_timeline.hipo
-> outhipo.$dataset/QA_timeline_epochs.hipo

=====================================

helicityPhiRead.groovy
-> outhipo/sinphi_${runnum}.hipo
// make sure that `.` is in `$CLASSPATH`

   |
   V

helicityPhiPlot.groovy
